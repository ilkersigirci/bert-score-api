version: "3.9"

services:
  bert-score-api-dev:
    image: bert-score-api-dev:latest
    build:
      context: .
      dockerfile: docker/Dockerfile
      target: development
    # ports:
    #   - 8080:8080
  bert-score-api-prod:
    image: bert-score-api-prod:latest
    build:
      context: .
      dockerfile: docker/Dockerfile
      target: production
    # ports:
    #   - 8080:8080
